from __future__ import annotations

from niche.core.models.types import FeedBundle, Item


def translate(items: list[Item], bundle: FeedBundle) -> list[Item]:
    """
    M1 stub: passes items through unchanged.
    Real DeepL + Haiku translation added in M4.
    """
    return items
